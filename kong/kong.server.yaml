_format_version: "3.0"
_transform: true

services:
  - name: nextjs-service
    url: http://server:3000 
    routes:
      - name: nextjs-route
        paths:
          - /*
        methods: ["GET", "POST", "PUT", "DELETE"]  # Allowed HTTP methods
